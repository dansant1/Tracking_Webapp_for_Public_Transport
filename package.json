{
  "name": "tpi",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "DEPLOY_HOSTNAME=galaxy.meteor.com meteor deploy tpi.grupoddv  --settings settings.json"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "bcrypt": "^1.0.2",
    "meteor-node-stubs": "^0.2.5",
    "node-xlsx": "^0.7.4",
    "simpl-schema": "^0.1.0"
  }
}
